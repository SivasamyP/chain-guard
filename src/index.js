import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import SupplierDashboard from './supplierchainguard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SupplierDashboard />
  </React.StrictMode>
);


